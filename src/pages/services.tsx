import DefaultLayout from "@/layouts/default";

export default function ServicesPage() {
	return (
		<DefaultLayout>
			<section className="w-dvw flex flex-col items-center justify-center gap-4 py-4">
				<div>Servicios</div>
			</section>
		</DefaultLayout>
	);
}
